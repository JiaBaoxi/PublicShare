    Get key for AI model encryption/decryption
    ---
    parameters:
      - name: model
        in: path
        type: string
        required: true
        description: model name
      - name: version
        in: path
        type: string
        required: true
        description: model version
    responses:
      200:
        description: Get key successfully
      404:
        description: Key is not found